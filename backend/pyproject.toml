[project]
name = "backend"
version = "0.1.0"
description = ""
authors = [{ name = "Matthew Li", email = "matthew.li10@education.nsw.gov.au" }]
readme = "README.md"
requires-python = ">=3.13"
dependencies = ["cryptography (>=46.0.3,<47.0.0)", "python-dotenv (>=1.2.1,<2.0.0)", "quart[dotenv] (>=0.20.0,<0.21.0)", "aiortc (>=1.14.0,<2.0.0)", "quart-cors (>=0.8.0,<0.9.0)", "requests (>=2.32.5,<3.0.0)"]

[tool.poetry]
packages = [{ include = "inter", from = "src" }]

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
